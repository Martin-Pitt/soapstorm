<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<!-- <SS:Nexii> Info views (doc/atmo_magic_debug_views.md): pick ONE curated
     mode, the world dims, one data layer pops, the legend bottom-left says what
     the colours mean, and the chart below shows what a 3D overlay cannot. The
     engineering checkboxes on the Overlays tab stay untouched underneath; a
     mode may drive them (V1 turns the flow arrows on for its near-ground layer)
     and restores them on the way out. Everything here is read-only: no view
     ever triggers a build. Rects follow the stretch rule - explicit left and
     right, never left_pad with right. -->
<panel
 name="debug_views_panel"
 follows="all"
 layout="topleft"
 background_visible="false"
 border="false"
 top="0"
 left="0"
 height="400"
 width="412">

    <text
     follows="left|top"
     layout="topleft"
     left="12"
     top="8"
     height="14"
     width="382"
     name="views_label"
     font="SansSerifSmallBold">
        Info view
    </text>
    <text
      follows="left|top" layout="topleft"
      left="12" top_pad="6" height="16" width="106"
      name="T_info_view"
      tool_tip="One curated view at a time. Off leaves every overlay exactly as the Overlays tab has it">
        Mode:
    </text>
    <combo_box
      control_name="SSAtmoInfoView" name="C_info_view"
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="-2" height="20" width="250"
      tool_tip="Off: no dimming, no legend, no chart. Wind Profile (V1): the altitude wind profile - the boundary-layer curve with rails at 10m, the 1500m boundary-layer top, the deck base and lid and the cirrus band's CURRENT altitude, live speed and heading at each, a hodograph inset, and an in-world wind mast ahead of the camera whose arrow stack twists with the shear">
        <combo_box.item label="Off" value="0" name="info_view_off" />
        <combo_box.item label="Wind Profile (V1)" value="1" name="info_view_wind" />
    </combo_box>

    <text
      follows="left|top" layout="topleft"
      left="12" top_pad="8" height="16" width="106"
      name="T_info_dim"
      tool_tip="How far the world darkens under the active view, so the data layer reads over it">
        World dim:
    </text>
    <slider_bar
      control_name="SSAtmoInfoViewDim" name="SB_info_dim"
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="0" height="16" width="160" min_val="0" max_val="0.95" increment="0.05"
      tool_tip="How far the world darkens under the active view, so the data layer reads over it" />
    <spinner
      control_name="SSAtmoInfoViewDim" name="S_info_dim"
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="-2" height="20" width="62"
      label_width="0" min_val="0" max_val="0.95" increment="0.05" decimal_digits="2"
      tool_tip="How far the world darkens under the active view, so the data layer reads over it" />
    <button
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="0" height="20" width="24"
      name="D_info_dim" label="D"
      tool_tip="Reset to the default value">
        <button.commit_callback function="ResetControl" parameter="SSAtmoInfoViewDim" />
    </button>

    <text
     follows="left|top"
     layout="topleft"
     left="12"
     top_pad="12"
     height="14"
     width="382"
     name="wind_profile_label"
     font="SansSerifSmallBold">
        Wind Profile options
    </text>
    <text
      follows="left|top" layout="topleft"
      left="12" top_pad="6" height="16" width="106"
      name="T_mast_offset"
      tool_tip="Metres ahead of the camera the wind mast stands. 0 stands it on the camera column itself, which reads best looking straight up">
        Mast offset:
    </text>
    <slider_bar
      control_name="SSAtmoInfoViewMastOffset" name="SB_mast_offset"
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="0" height="16" width="160" min_val="0" max_val="500" increment="10"
      tool_tip="Metres ahead of the camera the wind mast stands. 0 stands it on the camera column itself, which reads best looking straight up" />
    <spinner
      control_name="SSAtmoInfoViewMastOffset" name="S_mast_offset"
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="-2" height="20" width="62"
      label_width="0" min_val="0" max_val="2000" increment="10" decimal_digits="0"
      tool_tip="Metres ahead of the camera the wind mast stands. 0 stands it on the camera column itself, which reads best looking straight up" />
    <button
      follows="left|top" layout="topleft"
      left_pad="4" top_delta="0" height="20" width="24"
      name="D_mast_offset" label="D"
      tool_tip="Reset to the default value">
        <button.commit_callback function="ResetControl" parameter="SSAtmoInfoViewMastOffset" />
    </button>
    <check_box
      control_name="SSAtmoInfoViewCompare" name="CB_compare"
      follows="left|top" layout="topleft"
      left="12" top_pad="6" height="16" width="380"
      label="Compare exponent sources (cube vs flowmap region vs gradient setting)"
      tool_tip="Overlays the curves the flowmap's camera-region roughness exponent and the SSAtmoWindFlowGradient fallback WOULD give beside the live weather-cube curve, so which source is live and what each would do is visible at once. Only the cube's curve ever drives the sky" />

    <ss_atmo_graph_view
      name="info_graph"
      follows="all"
      layout="topleft"
      left="12"
      right="-12"
      top="152"
      bottom="-8"
      mouse_opaque="false" />
</panel>
